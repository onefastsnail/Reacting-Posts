//we keep these here as we use them in 2 locations, one with an action to send to the dispatcher, secondly to listen from the store for that action type
var appConstants = {
  POSTS_FILTER_POSTS: "POSTS_FILTER_POSTS",
  POSTS_GET_POSTS: "POSTS_GET_POSTS",
  POSTS_SET_POSTS: "POSTS_SET_POSTS",
};

module.exports = appConstants;